diff --git a/node_modules/@elevenlabs/client/dist/BaseConversation.js b/node_modules/@elevenlabs/client/dist/BaseConversation.js
index 9a10aa6..a5de8d8 100644
--- a/node_modules/@elevenlabs/client/dist/BaseConversation.js
+++ b/node_modules/@elevenlabs/client/dist/BaseConversation.js
@@ -255,8 +255,13 @@ export class BaseConversation {
         }
     }
     handleErrorEvent(event) {
-        const errorType = event.error_event.error_type;
-        const message = event.error_event.message || event.error_event.reason || "Unknown error";
+        const err = event?.error_event;
+        if (!err) {
+            this.onError("Server error", event);
+            return;
+        }
+        const errorType = err.error_type;
+        const message = err.message || err.reason || "Unknown error";
         if (errorType === "max_duration_exceeded") {
             void this.endSessionWithDetails({
                 reason: "error",
@@ -269,9 +274,9 @@ export class BaseConversation {
         }
         this.onError(`Server error: ${message}`, {
             errorType,
-            code: event.error_event.code,
-            debugMessage: event.error_event.debug_message,
-            details: event.error_event.details,
+            code: err.code,
+            debugMessage: err.debug_message,
+            details: err.details,
         });
     }
     onMessage = async (parsedEvent) => {
diff --git a/node_modules/@elevenlabs/client/dist/lib.iife.js b/node_modules/@elevenlabs/client/dist/lib.iife.js
index d4aa299..47eb766 100644
--- a/node_modules/@elevenlabs/client/dist/lib.iife.js
+++ b/node_modules/@elevenlabs/client/dist/lib.iife.js
@@ -23230,8 +23230,13 @@ registerProcessor("scribeAudioProcessor", ScribeAudioProcessor);
 			if (this.options.onGuardrailTriggered) this.options.onGuardrailTriggered();
 		}
 		handleErrorEvent(event) {
-			const errorType = event.error_event.error_type;
-			const message = event.error_event.message || event.error_event.reason || "Unknown error";
+			const err = event?.error_event;
+			if (!err) {
+				this.onError("Server error", event);
+				return;
+			}
+			const errorType = err.error_type;
+			const message = err.message || err.reason || "Unknown error";
 			if (errorType === "max_duration_exceeded") {
 				this.endSessionWithDetails({
 					reason: "error",
@@ -23244,9 +23249,9 @@ registerProcessor("scribeAudioProcessor", ScribeAudioProcessor);
 			}
 			this.onError(`Server error: ${message}`, {
 				errorType,
-				code: event.error_event.code,
-				debugMessage: event.error_event.debug_message,
-				details: event.error_event.details
+				code: err.code,
+				debugMessage: err.debug_message,
+				details: err.details
 			});
 		}
 		onMessage = async (parsedEvent) => {
